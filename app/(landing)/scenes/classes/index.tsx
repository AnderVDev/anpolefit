import React from "react";
import { generalContent, mainBackground } from "@/lib/styles"; 

// type Props = {}

// function index({}: Props) {
function index() {
  return (
    <>
      <div className={`${generalContent} bg-darkpurple`}>Our Clases</div>
    </>
  );
}

export default index;
